spring.application.name=server

# MySQL Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/classlink_db
spring.datasource.username=root
spring.datasource.password=gwapo123

spring.jpa.hibernate.ddl-auto=update

# CORS/Frontend origin for credentials-based requests
app.cors.allowed-origin=http://localhost:5173,http://localhost:5174

# Session cookie tweaks suitable for local dev
# SameSite=Lax works for localhost different ports; set None if using HTTPS on a different domain
server.servlet.session.cookie.same-site=Lax
server.servlet.session.cookie.secure=false