// Backwards-compatible wrapper to the new ProfilePage.
// Keeps existing imports working while the app migrates to `/profile`.
import ProfilePage from './ProfilePage.jsx';

export default ProfilePage;